{
  "name": "kor-4096",
  "description": "Hangul base-11172 encoder/decoder using Korean syllables",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/hexKor.d.ts",
  "license": "MIT",
  "author": "ksPr",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "oxlint": "^1.57.0",
    "oxfmt": "^0.42.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {},
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "clean": "rm -rf dist",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "build:js": "bun build src/hexKor.ts --minify --target=bun --outfile dist/index.js",
    "build": "bun run clean && bun run build:types && bun run build:js",
    "lint": "oxlint ./src",
    "fmt": "oxfmt ./src",
    "fmt:check": "oxfmt --check ./src",
    "check": "bun run lint && bun run fmt:check && bun run build"
  }
}
